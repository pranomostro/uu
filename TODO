-play around with paralellism and look if it gets any faster.
-write a version in go.
-optimize nalread
-afind should be a mixture of the current afind and binary search. bsearch() with approximating the next position.
