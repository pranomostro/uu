-find a way to make the bhashuu hashes better.
	Probably use a perfect hashing function?
-with really big inputs, bhashuu begins to ignore different numbers of lines.
	Especially with different optimizations. Check this with valgrind.
-add automated tests
