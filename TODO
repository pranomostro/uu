-find a way to make the bhashuu hashes better.
	Probably use a perfect hashing function?
	Or use a 64-bit hash.
-add automated tests
-make the input size variable
-improve the searching algorithm a little bit
