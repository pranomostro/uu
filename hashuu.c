/*unsorted uniq with hashes in an array that is shifted for inserting*/

#include <stdlib.h>
#include <stdio.h>

long hash(char* s, size_t len);
size_t binfind(long key, int* data, size_t len);

int main(int argc, char** argv)
{
	return 0;
}

long binfind(long key, int* data, size_t len)
	{}
